---

- name: Stop docker
  systemd:
    name: docker
    state: stopped

- name: Clean docker config
  file:
    path: '{{ item.root/item.path }}'
    state: absent
  with_filetree:
    - '/var/lib/docker/'
