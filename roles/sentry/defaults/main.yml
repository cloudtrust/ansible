git_tag: initial
archive_name: image
git_repo: git@github.com:cloudtrust/sentry-service.git

config_repo_name: dev
config_repository: git@github.com:cloudtrust/dev-config.git
config_branch: master
config_env: ""

remote_path: /cloudtrust/sentry

dockerfile: cloudtrust-sentry.dockerfile
image_name: 'cloudtrust-sentry:{{ git_tag }}-{{ config_repo_name }}-{{ config_branch }}'

pull_path: '{{ role_path }}/sentry'

base_name: sentry

dockerfile_path: '{{ pull_path }}/dockerfiles'
context: '{{ pull_path }}/build_context'

server_name: sentry.cloudtrust.com

certs_path: '{{ role_path }}/files'
tls_crt: '{{ certs_path }}/tls.crt'
tls_key: '{{ certs_path }}/tls.key'
tls_secret_name: sentry-tls
