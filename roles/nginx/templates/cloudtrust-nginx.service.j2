[Unit]
Description=Nginx container
After=docker.service

[Service]
Type=simple
ExecStart=/usr/bin/docker start -i -a {{ container_name }}
ExecStop=/usr/bin/docker stop {{ container_name }}

[Install]
WantedBy=multi-user.target

