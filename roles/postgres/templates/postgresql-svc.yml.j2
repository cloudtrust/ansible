apiVersion: v1
kind: Service
metadata:
  name: postgresql
spec:
  ports:
  - port: 5432
    protocol: TCP
    targetPort: postgresql
    name: postgresql
  type: ClusterIP
  selector:
    app: postgresql
